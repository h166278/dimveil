# Privacy

Dim Veil is offline by design. It does not request Internet access and contains no advertising SDK, analytics, account system, cloud synchronization, crash reporting service, or tracking identifier.

Only local display preferences (selected mode and dim depth) are saved with Android DataStore. The app never reads screen content, user input, accessibility node content, or other application data.
