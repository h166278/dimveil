# Accessibility overlay

The optional service is named **暗幕全屏覆盖服务**. It exists only to render a black, touch-through dim layer in the accessibility-overlay window type.

It does not retrieve window content, inspect accessibility nodes, perform gestures, click controls, control other applications, or monitor input. Enabling it is always a manual Android Settings action.
